spring:
  application:
    name: blog_dos_devs
  datasource:
    url: jdbc:postgresql://localhost:5432/blog_db
    username: blog_user
    password: pass321
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true